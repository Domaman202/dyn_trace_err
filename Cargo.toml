[package]
name = "dyn_trace_err"
description = "Dynamic errors with a stack trace."
license = "MIT"
version = "0.1.0"
edition = "2024"

[features]
default = ["all-trace"]
all-trace = []
my-trace = []
no-trace = []

[dependencies]
